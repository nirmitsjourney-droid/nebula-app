# Nebula Backend Server Configuration
# ~/.nebula/config.toml is auto-created on first run with defaults.
# This file in the repo directory serves as an example / fallback.

input_port = 3001
output_port = 3002

[addon]
enabled = true

[agent]
command = "echo"
args = []
mode = "stdio"
